{% extends 'base.html.twig' %}

{% block title %}Journee 1{% endblock %}

{% block body %}

    <div class="row">
        <div class="col s1"></div>
        <div class="col s9">
            <div class="center titre">
                <h2 class="titre_txt">Journée {{ app.request.pathinfo | last}}</h2>
            </div>

            {% if app.user %}
                <div class="card-panel center">

                    <h5>Etes-vous disponible pour cette journée ?</h5>

                    {% if disposJoueur is empty %}
                        <p>Vous n'êtes pas encore inscrit</p>
                        <a href="{{ path('journee.disponibilite.new', {journee: journee.idJournee, dispo: 1}) }}">
                            <button class="btn waves-effect green lighten-2 waves-light" type="submit" name="action">Se déclarer disponible</button>
                        </a>
                        <a href="{{ path('journee.disponibilite.new', {journee: journee.idJournee, dispo: 0}) }}">
                            <button class="btn waves-effect red lighten-2 waves-light" type="submit" name="action">Se déclarer indisponible</button>
                        </a>
                    {% elseif not disposJoueur[0].disponibilite  %}
                        <p>Vous vous êtes inscrit comme étant <span class="red-text lighten-3"><b>indisponible</b></span></p>
                        <a href="{{ path('journee.disponibilite.update', {journee: journee.idJournee, disposJoueur: disposJoueur[0].idDisponibilite, dispo: 1}) }}">
                            <button class="btn waves-effect green lighten-2 waves-light" type="submit" name="action">Se déclarer disponible</button>
                        </a>
                    {% elseif disposJoueur[0].disponibilite %}
                        <p>Vous vous êtes inscrit comme étant <span class="green-text lighten-3"><b>disponible</b></span></p>
                        <a href="{{ path('journee.disponibilite.update', {journee: journee.idJournee, disposJoueur: disposJoueur[0].idDisponibilite, dispo: 0}) }}">
                            <button class="btn waves-effect red lighten-2 waves-light" type="submit" name="action">Se déclarer indisponible</button>
                        </a>
                    {% endif %}
                </div>
            {% else %}
                <div class="card-panel green lighten-4 center">
                    <p>Vous devez vous connecter pour indiquer votre disponibilité pour cette journée.</p>
                    <a href="{{ path('login') }}">
                        <button class="btn waves-effect waves-light" type="submit" name="action">Se connecter</button>
                    </a>
                </div>
            {% endif %}

            {% for compo in compos %}
                <div class="cadre center">
                    <div class="row">
                        <h5 class="col s2">Equipe n°{{ compo.idEquipe }}</h5>
                        <h5 class="col s3">Journée n°{{ compo.idJournee.nJournee }}</h5>
                        <h5 class="col s3">{{ compo.idJournee.date | date("m/d/Y")}}</h5>
                        <h5 class="col s4">{{ compo.idJournee.lieu }}</h5>
                    </div>

                    <div class="row">
                        <div class="col s9">
                            <table class="centered">
                                <thead>
                                <tr>
                                    <th></th>
                                    <th>Composition</th>
                                </tr>
                                </thead>
                                <tbody>
                                <tr>
                                    <td><b>#1</b></td>
                                    <td>
                                        {% if compo.idJoueur1 is not null %}
                                            {{ compo.idJoueur1.nom }} {% if compo.idJoueur1.role == "C" %}<span class="new badge red" data-badge-caption="Capitaine"></span>{% endif %}
                                        {% endif %}
                                    </td>
                                </tr>

                                <tr>
                                    <td><b>#2</b></td>
                                    <td>
                                        {% if compo.idJoueur2 is not null %}
                                            {{ compo.idJoueur2.nom }} {% if compo.idJoueur2.role == "C" %}<span class="new badge red" data-badge-caption="Capitaine"></span>{% endif %}
                                        {% endif %}
                                    </td>
                                </tr>

                                <tr>
                                    <td><b>#3</b></td>
                                    <td>
                                        {% if compo.idJoueur3 is not null %}
                                            {{ compo.idJoueur3.nom }} {% if compo.idJoueur3.role == "C" %}<span class="new badge red" data-badge-caption="Capitaine"></span>{% endif %}
                                        {% endif %}
                                    </td>
                                </tr>

                                <tr>
                                    <td><b>#4</b></td>
                                    <td>
                                        {% if compo.idJoueur4 is not null %}
                                            {{ compo.idJoueur4.nom }} {% if compo.idJoueur4.role == "C" %}<span class="new badge red" data-badge-caption="Capitaine"></span>{% endif %}
                                        {% endif %}
                                    </td>
                                </tr>
                                </tbody>
                            </table>
                        </div>
                        {% if compo.idEquipe != 1 %}
                            <div class="col s3">
                                <table class="centered">
                                    <thead>
                                    <tr>
                                        <th>Joueurs brûlés</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    <tr>
                                        <td>
                                            {% if compo.idJoueur1 is not null %}
                                                {{ compo.idJoueur1.nom }} {% if compo.idJoueur1.role == "C" %}<span class="new badge red" data-badge-caption="Capitaine"></span>{% endif %}
                                            {% endif %}
                                        </td>
                                    </tr>

                                    <tr>
                                        <td>
                                            {% if compo.idJoueur2 is not null %}
                                                {{ compo.idJoueur2.nom }} {% if compo.idJoueur2.role == "C" %}<span class="new badge red" data-badge-caption="Capitaine"></span>{% endif %}
                                            {% endif %}
                                        </td>
                                    </tr>

                                    <tr>
                                        <td>
                                            {% if compo.idJoueur3 is not null %}
                                                {{ compo.idJoueur3.nom }} {% if compo.idJoueur3.role == "C" %}<span class="new badge red" data-badge-caption="Capitaine"></span>{% endif %}
                                            {% endif %}
                                        </td>
                                    </tr>

                                    <tr>
                                        <td>
                                            {% if compo.idJoueur4 is not null %}
                                                {{ compo.idJoueur4.nom }} {% if compo.idJoueur4.role == "C" %}<span class="new badge red" data-badge-caption="Capitaine"></span>{% endif %}
                                            {% endif %}
                                        </td>
                                    </tr>
                                    </tbody>
                                </table>
                            </div>
                        {% endif %}
                    </div>

                    {% if 'ROLE_CAPITAINE' in app.token.roleNames %}
                        <a href="{{ path('journee.edit', {id: compo.id}) }}"
                           class="btn waves-effect waves-light blue lighten-2">Modifier la compo
                        </a>
                    {% endif %}
                </div>
            {% endfor %}
        </div>
        <div class="col s2">
            <table class="centered cadre dispos">
                <thead>
                    <tr>
                        <th>Les dispos</th>
                    </tr>
                </thead>

                <tbody>
                {% for dispo in dispos %}
                    <tr>
                        <td class="{% if dispo.disponibilite %} green-text lighten-3 {% elseif not dispo.disponibilite %} red-text lighten-3 {% endif %}">
                            {{ dispo.idCompetiteur.nom }}
                        </td>
                    </tr>
                {% endfor %}
                {% for dispoNonDeclaree in joueursNonDeclares %}
                    <tr>
                        <td class="grey-text lighten-3">
                            {{ dispoNonDeclaree.nom }}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}
