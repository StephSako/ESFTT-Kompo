{% extends 'base.html.twig' %}

{% block title %}Nouvelle phase{% endblock %}

{% block body %}

    {{ include('components/navbar_backoffice.html.twig') }}

    {% for message in app.flashes('success') %}
        {{ _flash.message(message, 'green') }}
    {% endfor %}

    {% for message in app.flashes('fail') %}
        {{ _flash.message(message, 'red') }}
    {% endfor %}

    <div class="container fftt_api_manager">
        <div class='card-panel center-align white-text title_journee'>
            <h3 style="margin-bottom: 15px; margin-top: 15px;">Recommencer une nouvelle phase</h3>
        </div>

        <form class="center" action="{{ path('backoffice.reset.phase') }}" method="POST" onsubmit="return confirm('Êtes-vous sûr de recommencer une nouvelle phase ?')">
            <input type="hidden" name="resetPhase" value="1">
            <button class='btn waves-effect waves-light green lighten-2 btn_gestion'>Tout mettre à jour</button>
        </form>

        <div class='card-panel center'>
            <h4>Les compétiteurs</h4>
            <p><b>{{ messageJoueurs }}</b></p>

            {% if joueursIssued|length %}
                <table class="centered responsive-table">
                    <thead>
                    <tr>
                        <th>Licence</th>
                        <th>Nom</th>
                        <th>Points officiels</th>
                    </tr>
                    </thead>

                    <tbody>
                        {% for joueur in joueursIssued %}
                            <tr>
                                <td>{{ joueur.joueur.licence }}</td>
                                <td>
                                    <div class="chip">
                                        {% if joueur.joueur.avatar %}
                                            <img src="{{ vich_uploader_asset(joueur.joueur, 'imageFile') }}" alt="Avatar">
                                        {% else %}
                                            <img src="{{ asset('images/account.png') }}" alt="Avatar">
                                        {% endif %}
                                        <span {% if not joueur.sameName %} class="red-text"{% endif %}>{{ joueur.joueur.nom }} {{ joueur.joueur.prenom }}</span>
                                        {% if not joueur.sameName %}<i class="material-icons black-text" style="vertical-align: sub;">arrow_forward</i>{{ joueur.nomFFTT }}{% endif %}
                                    </div>
                                </td>
                                <td>
                                    <span {% if joueur.joueur.classementOfficiel != joueur.pointsFFTT %}class="red-text"{% endif %}>{{ joueur.joueur.classementOfficiel }}</span>
                                    {% if joueur.joueur.classementOfficiel != joueur.pointsFFTT %}
                                        <i class="material-icons black-text">arrow_forward</i> {{ joueur.pointsFFTT }}
                                    {% endif %}
                                </td>
                                <td>

                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            {% endif %}
        </div>

        <div class="card-panel center">
            <h4>Les équipes - Départemental</h4>
            <p><b>{{ messageEquipes }}</b></p>

            {% if equipesIssued|length %}
                    <table class="striped centered responsive-table">
                        <thead>
                        <tr>
                            <th>Numéro</th>
                            <th>Division</th>
                            <th>Poule</th>
                        </tr>
                        </thead>

                        <tbody>
                            {% for numero, equipe in equipesIssued %}
                                <tr>
                                    <td><b>{{ equipe.equipe.numero }}</b></td>
                                    <td>
                                        <span {% if not equipe.sameDivision %}class="red-text" {% endif %}>{{ equipe.equipe.idDivision.shortName }}</span>
                                        {% if not equipe.sameDivision %}<i class="material-icons black-text">arrow_forward</i>{{ equipe.divisionFFTT }}{% endif %}
                                    </td>
                                    <td>
                                        <span {% if not equipe.samePoule %}class="red-text" {% endif %}>{{ equipe.equipe.idPoule.poule }}</span>
                                        {% if not equipe.samePoule %}<i class="material-icons black-text">arrow_forward</i>{{ equipe.pouleFFTT }}{% endif %}
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
            {% else %}
                <p class="lobster_2 missingTitle">Il n'y a pas d'équipes incorrectes.</p>
            {% endif %}
        </div>

        <div class="card-panel center">
            <h4>Les rencontres - Départemental</h4>
            <p><b>{{ messageRencontres }}</b></p>

            {% if rencontresParEquipesSorted|length %}
                {% for equipe, rencontres in rencontresParEquipesSorted %}
                    <ul class="collapsible backoffice_disponibilites">
                        <li>
                            <div class="collapsible-header">
                                <div class="chip">
                                    <i class="material-icons" style="vertical-align: middle;">people</i>
                                    {{ equipe }}
                                </div>
                                <i class="caret material-icons">keyboard_arrow_down</i>
                            </div>
                            <div class="collapsible-body">

                                <table class="striped centered responsive-table">
                                    <thead>
                                    <tr>
                                        <th>Journée</th>
                                        <th>Adversaire</th>
                                        <th>Lieu</th>
                                    </tr>
                                    </thead>

                                    <tbody>
                                    {% for rencontre in rencontres %}
                                        <tr>
                                            <td><b>{{ rencontre.journee }}</b></td>
                                            <td>
                                                <span {% if rencontre.adversaireKompo != rencontre.adversaireFFTT %}class="red-text"{% endif %}>{{ rencontre.adversaireKompo }}</span>
                                                {% if rencontre.adversaireKompo != rencontre.adversaireFFTT %}<i class="material-icons black-text">arrow_forward</i>{{ rencontre.adversaireFFTT }}{% endif %}
                                            </td>
                                            <td>
                                                <span {% if rencontre.domicileKompo != rencontre.domicileFFTT %}class="red-text" {% endif %}>{{ rencontre.domicileKompo ? "<i class='material-icons'>home</i>" : "<i class='material-icons'>directions_car</i>" }}</span>
                                                {% if rencontre.domicileKompo != rencontre.domicileFFTT %}<i class="material-icons black-text">arrow_forward</i>{{ rencontre.domicileFFTT ? "<i class='material-icons'>home</i>" : "<i class='material-icons'>directions_car</i>" }}{% endif %}
                                            </td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </li>
                    </ul>
                {% endfor %}
            {% else %}
                <p class="lobster_2 missingTitle">Il n'y a pas de rencontres incorrectes.</p>
            {% endif %}
        </div>
    </div>

{% endblock %}